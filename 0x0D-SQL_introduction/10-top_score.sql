-- Lists all records of the table second_table of a
-- database passed as an argument of the mysql command.
-- Results should display both the score and the name
-- in that order. Record should be ordered by
-- score (top first).
SELECT score, name FROM second_table
ORDER BY score DESC;
